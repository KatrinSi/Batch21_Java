package Replit;

public class Replit_176 {
    public boolean simpleRoomBook(boolean isAvailable,int month,
                                  int day, int year)
    {

        boolean isTrue = false;
        if(isAvailable == true && year == 2018||month == 7 && day>8){

        isTrue = true;

    }
        return isTrue;
    }
}
